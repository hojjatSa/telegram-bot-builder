# Шаблон `code`

Выполняет пользовательский Python как тело async-функции. В пространстве имён — переменные пользователя, `client` / `userbot_client`, `callback_query`.

Исходник передаётся строкой (`tojson`), не вклеивается в файл бота. Таймаут 180 с. Ошибка не рвёт цепочку автоперехода.
